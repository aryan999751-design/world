export function correlate(events,{radiusKm=250,windowMs=60*60*1000}={}){const out=[];for(const a of events){const hits=[];for(const b of events){if(a===b)continue;const dt=Math.abs(new Date(a.time)-new Date(b.time));if(dt>windowMs)continue;const d=distanceKm(a.lat,a.lon,b.lat,b.lon);if(d<=radiusKm)hits.push({...b,distanceKm:d,timeDeltaMs:dt})}if(hits.length)out.push({...a,correlated:hits})}return out}function distanceKm(aLat,aLon,bLat,bLon){const r=6371,dLat=(bLat-aLat)*Math.PI/180,dLon=(bLon-aLon)*Math.PI/180;const x=Math.sin(dLat/2)**2+Math.cos(aLat*Math.PI/180)*Math.cos(bLat*Math.PI/180)*Math.sin(dLon/2)**2;return 2*r*Math.asin(Math.sqrt(x))}