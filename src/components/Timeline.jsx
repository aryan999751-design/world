import React from "react";
export default function Timeline({open,playing,setPlaying,time,setTime,min,max,speed,setSpeed,mode,setMode}){if(!open)return null;const p=((time-min)/(max-min))*100;return <div className="timeline"><div className="tlhead"><div><div className="k">4D PLAYBACK</div><b>{mode==="replay"?"REPLAY":"LIVE MONITOR"}</b></div><div className="tlbuttons"><button className={mode==="replay"?"sel":""} onClick={()=>setMode("replay")}>REPLAY</button><button className={mode==="live"?"sel":""} onClick={()=>setMode("live")}>LIVE</button><button onClick={()=>setPlaying(x=>!x)}>{playing?"PAUSE":"PLAY"}</button></div></div><div className="readout">{new Date(time).toISOString()} · {speed}×</div><input className="range" type="range" min={min} max={max} value={time} onChange={e=>setTime(+e.target.value)} style={{"--p":p+"%"}}/><div className="speeds">{[1,5,15,60].map(x=><button className={speed===x?"sel":""} key={x} onClick={()=>setSpeed(x)}>{x}×</button>)}</div></div>}
